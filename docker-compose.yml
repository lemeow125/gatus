services:
  gatus:
    restart: no
    image: twinproduction/gatus:v5.36.0
    ports:
      - "${PORT}:8080"
    volumes:
      - ./config:/config
    environment:
      - GATUS_CONFIG_PATH=/config
    env_file: .env